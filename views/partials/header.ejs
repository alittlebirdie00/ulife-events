<!DOCTYPE html>

<html>
    <head>
        
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Ulife</title>
        <link rel="stylesheet" href="/stylesheets/stylesheet.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bulma/0.7.1/css/bulma.css">
        <link href="https://fonts.googleapis.com/css?family=Lato:300" rel="stylesheet">
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.3.1/css/all.css" integrity="sha384-mzrmE5qonljUremFsqc01SB46JvROS7bZs3IO2EmfFsd15uHvIt+Y8vEf7N7fWAU" crossorigin="anonymous">
        <script defer src="https://use.fontawesome.com/releases/v5.1.0/js/all.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/vue@2.5.17/dist/vue.js"></script>

        
    </head>
    <body>
        
    <!-- NAVBAR -->
    <nav class="navbar" role="navigation">
        
        <!-- Brand Logo Button (Upper Left of Navbar) -->
        <div class="navbar-brand">
            <a class="navbar-item" href="/">
                ULIFE
                <!-- <img src="https://bulma.io/images/made-with-bulma.png" width="128" height="24"> -->
            </a>
                
            <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
            </a>
        </div>
        
        <!-- Home Button (Upper Left of Navbar) -->
        <div id="navbarBasicExample" class="navbar-menu">

            <% if (userInSession != undefined) { %>
                <% if (userInSession.userLevel == 'Super') { %>
                    <div class="navbar-start">
                        <a class="navbar-item">
                            Create University Profile
                        </a>
                    </div>                 
                <% } else if (userInSession.userLevel == 'Admin') { %>
                    <div class="navbar-start">
                        <a class="navbar-item">
                            Create an RSO
                        </a>
                    </div>                    
                <% }%>           
            <% } %>
            
            <div class="nabar-end">
                <div class="navbar-item">
                    <div class="buttons">
                        <% if (isAuthenticated) { %>
                            <a class="button is-light" href="/profile/">
                            Profile
                            </a>
                            
                            <a class="button is-light" href="/logout">
                            Log Out
                            </a>
                        <% } else { %>
                            <a class="button is-primary" href="/signup">
                                Sign Up
                            </a>
                            
                            <a class="button is-light" href="/login">
                                Log In
                            </a>                        
                        <% } %>
                        
                    </div>
                </div>
            </div>
        </div>
    </nav>
